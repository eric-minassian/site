{
  "name": "site",
  "private": true,
  "description": "Free managed personal-site platform",
  "scripts": {
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r --filter '!@site/e2e' run test",
    "test:e2e": "pnpm --filter @site/e2e run test",
    "build": "pnpm -r --filter '!@site/e2e' run build"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@swc/core",
      "esbuild"
    ]
  }
}
